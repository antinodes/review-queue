{
  "name": "review-queue-infra",
  "version": "0.1.0",
  "private": true,
  "description": "CDK app: S3 + CloudFront + OAuth token-exchange Lambda for review-queue",
  "bin": {
    "review-queue-infra": "bin/app.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy"
  },
  "devDependencies": {
    "@types/node": "^22",
    "aws-cdk": "^2.1005.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.3.0"
  }
}
